import SwippableModal, {
  hideModal,
  modalRef,
  panRef,
  showModal,
} from './SwippableModal';

export type { SwippableModalProps, SwippableModalRefType } from './types';
export { modalRef, showModal, hideModal, SwippableModal, panRef };
